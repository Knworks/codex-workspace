---
id: CONFIG-001
title: 同期先フォルダ設定の追加
status: Done
---

# 🧾`CONFIG-001 同期先フォルダ設定の追加`

## 🎯背景/目的

- 同期機能の有効/無効と同期先をユーザー設定で制御できるようにするため。

## 📌要件

- `codexFolder` / `promptsFolder` / `skillsFolder` / `templatesFolder` を追加する。
- 既定値はブランクとする。
- 設定の説明文を用意する。

## 🛠️スコープ / 作業内容

- `package.json` に設定スキーマを追加する。
- 設定値取得の共通処理を用意する。
- 既存設定との整合性を確認する。

## ✅AC（受け入れ基準）

- [x] [機能] 4 つの設定キーが VS Code の設定として登録されている。
- [x] [機能] 既定値がブランクである。
- [x] [UI/UX] 設定項目に説明文が表示される。
- [x] [テスト] 設定値取得のユニットテストが追加されている。

## 🔗依存関係

- DependsOn: なし

## 🧪テスト観点

- (ユニット)
- 検証方法：設定値取得が空/非空で正しく分岐することを確認する。
