# 🧾`CORE-002 フォルダを開くコマンド`

## 🎯背景/目的

- `.codex` を OS のファイラーで即座に開けるようにし、外部操作を短縮する。

## 📌要件

- 5.8

## 🛠️スコープ / 作業内容

- `.codex` を開くコマンドを追加する
- パスが存在しない場合はエラーメッセージを表示する
- UI/イベント/保存先: UI 最上部の共通ボタンから OS を起動（保存先なし）
- 参照: `docs/01_requirements.md`, `docs/02_architect.md`

## ✅AC（受け入れ基準）

- [x] [機能] `.codex` を OS のエクスプローラ/Finder で開ける
- [x] [状態/エラー] 対象パスが存在しない場合はエラーメッセージが表示される
- [x] [テスト] コマンドが正しいパスで OS 起動 API を呼ぶことをテストで検証できる

## 🔗依存関係

- DependsOn: #2

## 🧪テスト観点

- (ユニット)
- 検証方法：`npm test` で OS 起動 API の呼び出しを検証する


