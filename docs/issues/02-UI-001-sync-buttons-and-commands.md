---
id: UI-001
title: 同期ボタンとコマンドの追加
status: Done
---

# 🧾`UI-001 同期ボタンとコマンドの追加`

## 🎯背景/目的

- 各 Explorer から同期を実行できる UI を提供するため。

## 📌要件

- Codex Core / Prompts / Skills / Templates に同期ボタンを追加する（codicon: `sync`）。
- 同期先フォルダ設定が空の場合はボタンを非表示とする。
- 同期コマンドを定義し、各ビューの view/title に紐付ける。

## 🛠️スコープ / 作業内容

- `package.json` に同期コマンドを追加する。
- view/title のメニューに同期ボタンを追加する。
- 設定値が空の場合にボタンを表示しない条件を追加する。

## ✅AC（受け入れ基準）

- [x] [UI/UX] 各 Explorer の view/title に同期ボタンが表示される。
- [x] [UI/UX] 同期先フォルダ設定が空の場合はボタンが表示されない。
- [x] [機能] 同期ボタンから同期コマンドが起動できる。
- [x] [テスト] 表示条件のテストが追加されている。

## 🔗依存関係

- DependsOn: CONFIG-001

## 🧪テスト観点

- (ユニット / リクエスト)
- 検証方法：設定値の空/非空に応じてボタン表示が切り替わることを確認する。
